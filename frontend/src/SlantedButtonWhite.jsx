const Button = styled.button`
background: #fff;
-ms-transform: skewX(-20deg);
 -webkit-transform: skewX(-20deg);
 transform: skewX(-20deg);
`;

return <Button>{props.text}</Button>;

/* I am experimenting with using this button. */